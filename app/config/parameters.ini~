; These parameters can be imported into other config files
; by enclosing the key with % (like %database_user%)
; Comments start with ';', as in php.ini
[parameters]
    database_driver   = pdo_mysql
    database_host     = localhost
    database_port     =
    database_name     = alienware
    database_user     = root
    database_password = park

    mailer_transport  = smtp
    mailer_host       = localhost
    mailer_user       =
    mailer_password   =
    mailer_encryption = tls
    mailer_port       = 587

    locale            = en

    secret            = ThisTokenIsNotSoSecretChangeIt

    recaptcha.public_key =
    recaptcha.private_key =

    base_host         = site.com
    host_suffix       =

    s3.access_key     = AKIAIEEZJDPZDWD4SDOA
    s3.secret_key     = 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
    s3_bucket_name    = platformd-testing

    ; this is for the "alienware" staging
    facebook_app_id   = 468808186479620

    mink_base_url     = http://your_app.dev/app_test.php

    google_api_key    = YourGoogleApiKey
