{#
    Reusable bit in the admin where you need the user to select a region

    Required variables are:
        * sites: from SiteManager->getSiteChoices()
        * route: the name of the route used in the link
#}

<div class="row-fluid">
    <div class="well span4" style="padding: 8px 0;">
        <ul class="nav nav-list">
            <li class="nav-header">
                Choose a Site
            </li>
            {% for id, site in sites %}
            <li>
                <a href="{{ path(route, {'site': id }) }}">{{ site.name }}</a>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>
