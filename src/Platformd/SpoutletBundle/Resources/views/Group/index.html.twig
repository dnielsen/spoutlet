{% extends 'SpoutletBundle::layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/spoutlet/css/groups.css') }}" />
{% endblock %}

{% block content %}

{% set site = get_current_site() %}
<div class="std_1col" style="padding: 0;">
    <img src="" alt="Alienware Arena Groups" style="width: 950px; height: 120px; border: none;" />
</div>
<div class="groups-sub-nav">
    <ul>
        <li><a href="{{ path('accounts_groups') }}">My Groups</a></li>
        <li><a href="{{ path('group_new') }}">Create Group</a></li>
    </ul>
</div>
<div class="std_2col">
    <div class="left">
        <h3 style="margin: 0; color: #fff;">Recommended ALIENWARE Groups</h3>
        <div class="scrollable" id="scrollable-location-groups">
            <div class="items">
                {% for page in locationGroups %}
                    <div class="groups-wrapper">
                    {% for groupList in page %}
                        {% for group in groupList %}
                            <div>{{ group.name }}</div>
                        {% endfor %}
                    {% endfor %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <div class="right">
        {% include 'SpoutletBundle:Default:_dealsAd.html.twig' %}
        <div class="widget-33">
            <div class="widget-header">
                <div class="widget-title">
                    Welcome New Groups
                </div>
            </div>
            <div class="widget-content">
                {% for recentGroup in recentGroups %}
                <div class="widget-item">
                    <a class="widget-link news-ellipse" href="{{ path('group_show', { 'id': recentGroup.id }) }}">{{ recentGroup.name }}</a>
                </div>
                {% else %}
                    <p>No groups found. Check back soon!</p>
                {% endfor %}
            </div>
        </div>
        <div class="widget-33">
            <div class="widget-header">
                <div class="widget-title">
                    Most Popular Groups
                </div>
            </div>
            <div class="widget-content">
                {% for popularGroup in popularGroups %}
                <div class="widget-item">
                    <a class="widget-link news-ellipse" href="{{ path('group_show', { 'id': popularGroup['group_id'] }) }}">{{ popularGroup['group_name'] }}</a>
                </div>
                {% else %}
                    <p>No groups found. Check back soon!</p>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
