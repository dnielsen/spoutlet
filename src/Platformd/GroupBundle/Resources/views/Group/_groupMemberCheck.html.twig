<script type="text/javascript">
    var user_is_group_member = {% if isMember %}true{% else %}false{% endif %};
    var user_is_group_owner = {% if isOwner %}true{% else %}false{% endif %};
    var user_is_group_applicant = {% if isApplicant %}true{% else %}false{% endif %};
    var groupAdmin = (user_is_logged_in && (is_admin || user_is_group_owner));
</script>
