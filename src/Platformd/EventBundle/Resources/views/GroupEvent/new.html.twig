{% extends 'SpoutletBundle::layout.html.twig' %}

{% block content %}

    <h1>Create Event</h1>

    <form action="{{ path('group_event_new', {'groupSlug': group.slug }) }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
        {% include 'EventBundle:GroupEvent:_form.html.twig' %}

        <div class="form-actions" style="float: left;">
            <button type="submit" class="btn btn-primary" formnovalidate>Create</button>
            <button id="cancel" class="btn btn-primary" rel="{{ path('groups') }}" style="margin-left: 10px;">Cancel</button>
        </div>
    </form>

    <script type="text/javascript">
        $(document).ready(function() {

            $('input.datetime-picker').datetimepicker({
                dateFormat: 'yy-mm-dd',
                showSecond: true
            });

        });
    </script>

{% endblock %}
