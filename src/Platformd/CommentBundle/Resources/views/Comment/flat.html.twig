{#

 This file is part of the FOSCommentBundle package.

 (c) FriendsOfSymfony <http://friendsofsymfony.github.com/>

 This source file is subject to the MIT license that is bundled
 with this source code in the file LICENSE.

#}

{% for node in pager %}
    {# 1, 2 row number is how the odd/even was done in the existing design #}
    {% include "FOSCommentBundle:Comment:show.html.twig" with {
        "comment": node,
        "altRowNumber": (loop.index is odd) ? '1' : '2'
    } %}
{% endfor %}

{{ pagerfanta(pager, 'default', {'routeName': routeName, 'routeParams': routeParams }) }}
