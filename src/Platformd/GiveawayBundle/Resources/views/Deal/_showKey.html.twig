{% extends "SpoutletBundle::_flashMessage.html.twig" %}
{% block type %}success{% endblock %}
{% block body %}
    <p>{{ 'deal_redeem_success_code' | trans }}</p>
    <p>
        {%- if data.deal_code_is_url -%}
            <a href='{{ data.deal_code_assigned | raw }}' target='_blank'>{{ data.deal_code_assigned | wrap | raw }}</a>
        {%- else -%}
            {{ data.deal_code_assigned }}
        {%- endif -%}
    </p>
    <p>{{ 'deal_redeem_success' | trans }}</p>
    {% if features.hasGroups %}
        <p>
            {% if data.deal_group_slug %}
                {% if data.is_member_of_deal_group %}
                    {{ 'deal_join_group_success' | trans | replace({'%link%': path('group_show', {'slug': data.deal_group_slug}), '%group%': data.deal_group_name}) | raw }}
                {% endif %}
            {% endif %}
        </p>
    {% endif %}
{% endblock %}
