<div class="newsBlurb">
    <h2>
        <a href="{{ path('news_show', {'slug': item.slug}) }}"></a>
        <a href="{{ item | pd_link }}">{{ item.title }}</a>
    </h2>
    <h6>
        {{ 'announced_date' | trans({
            '%posted_date%': ('sweepstakes_date_format' | trans(item.postedAtArray))
        })}}
    </h6>
    <p>{{ item.blurb }}</p>
    <a href="{{ item | pd_link }}">{{ 'read_more' | trans }}</a>
</div>