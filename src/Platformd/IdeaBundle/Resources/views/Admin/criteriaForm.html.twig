{% extends 'IdeaBundle::event_layout.html.twig' %}

{% block content %}

    <h1>{% if id is not null %}Edit{% else %}Submit{% endif %} Your Vote Criteria</h1>

    <form action="{{ path('idea_admin_criteria', {'groupSlug': groupSlug, 'eventSlug': eventSlug}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}

	    <input type="submit" value="Submit"/>
	    <input type="submit" value="Cancel" name="cancel" formnovalidate/>
	</form>
{% endblock %}
