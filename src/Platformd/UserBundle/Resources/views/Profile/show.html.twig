{% extends (isCurrentUser|default(false) ? 'UserBundle::accountLayout.html.twig' : 'SpoutletBundle::layout.html.twig') %}

{% block title %}{{ "profile.show.title" | trans({
                "%username%": user.username,
            }, 'FOSUserBundle') }}{% endblock %}

{% block content %}
{% include "FOSUserBundle:Profile:show_content.html.twig" %}
{% endblock content %}
